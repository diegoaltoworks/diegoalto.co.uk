export default function Template({ children }: { children: React.ReactNode }) {
	return (
		<>
			<br />
			this is the auth Template
			<br />
			this is the auth Template
			<br />
			this is the auth Template
			<br />
			this is the auth Template
			<br />
			this is the auth Template
			<br />
			this is the auth Template
			{children}
		</>
	);
}
